# --- General ---
add_newline = true

# --- Tool & Env Modules (The "Mise" View) ---
[golang]
symbol = "🐹 "
format = "via [$symbol($version )]($style)"
style = "bold cyan"

[python]
symbol = "🐍 "
format = "via [$symbol($version )]($style)"

[terraform]
symbol = "󱁢 "
format = "via [$symbol($workspace )]($style)"

# --- DevOps Modules ---
[kubernetes]
symbol = "☸️ "
disabled = false
format = 'at [$symbol$context( \($namespace\))]($style) '
style = "bold blue"

[aws]
symbol = "󰸏 "
format = 'on [$symbol($profile )(\($region\) )]($style)'

# --- Git Section ---
[git_branch]
symbol = " "
style = "bold purple"

[git_status]
style = "red"
format = '([\[$all_status$ahead_behind\]]($style) )'

# --- The Prompt ---
[character]
success_symbol = "[➜](bold green)"
error_symbol = "[➜](bold red)"