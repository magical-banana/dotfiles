# Powerlevel10k config — the actual theme settings live in ~/.p10k.zsh
# (stowed from zsh/.p10k.zsh). This loader file just sources it last so
# any prior overrides take effect first.
[[ -f "$HOME/.p10k.zsh" ]] && source "$HOME/.p10k.zsh"
